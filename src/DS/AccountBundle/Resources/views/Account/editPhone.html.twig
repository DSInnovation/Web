<h3>Modification du numéro de téléphone :</h3>

{{ form(form) }}

<script>
    function checkPhone() {
        if ( $('#form_tel').val().length < 10 ) {
            $('#form_Editer').attr("disabled", true);
        } else {
            $('#form_Editer').attr("disabled", false);
        }
    }
        
    $('#form_tel').keyup(function() {
        checkPhone();
    });
    
    $('#form_Annuler').click(function() {
        $('#modif_tel_window').hide();
    });
    
    $('#form_Editer').attr("disabled", true)
</script>